import ContactFormCore from './ContactFormCore';

/**
 * Direct contact form component - simplified for better reliability
 */
export default function LazyContactForm() {
  return <ContactFormCore />;
}